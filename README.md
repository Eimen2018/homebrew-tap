# homebrew-tap

Homebrew tap for [Lonar](https://github.com/Eimen2018/lonar) — sync external
monitor brightness with your MacBook's built-in display.

```bash
brew install --cask eimen2018/tap/lonar
```

Updates: Lonar updates itself via Sparkle (`auto_updates true`), or
`brew upgrade --cask lonar`.
